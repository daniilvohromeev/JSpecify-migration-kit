# Contributing

Use issues for bug reports, rule requests and recipe requests.

Before sending a pull request:

```bash
./gradlew build
```

Keep changes scoped to one feature or fix. Include tests for scanner, report,
recipe, CLI, Gradle plugin or Maven plugin behavior when changing those areas.
